<% content_for :title, @story.title + " - Learner Story" %>

<div class="story container">
  <h2><%= @story.title %></h2>
  <%= sanitizemarkdown(Kramdown::Document.new(@story.body).to_html)%>
 
</div>
<div class="container">
  <p><%=link_to "View more learner stories", stories_path, class: "btn btn-lil" %></p>
  <br />
</div>